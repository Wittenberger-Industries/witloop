---
type: Implementation Plan
title: "Issue sweep 3 — triage & sequencing plan for #35–#42 (+ late additions #47–#49)"
description: Priority/risk ordering, PR grouping, file-touch matrix, and guardrails for landing the 2026-07 observability + context-budget issues, extended 2026-07-11 with the late additions #47–#49; #34 postponed.
timestamp: 2026-07-10
tags: [triage, roadmap, context-budget, plan]
---

# Issue sweep 3 — triage & sequencing plan for #35–#42 (+ #47–#49)

**Goal:** land the 2026-07 observability + context-budget issues (#35–#42) in an order that maximizes measured savings while never touching a skill/agent file from two in-flight branches at once. #34 (cross-vendor MoA proposers) is **postponed** and out of scope.

**Status (updated 2026-07-11):** PR A merged (#44, v1.6.0) · PR B merged (#45, v1.7.0) · PR C merged
(#46, v1.7.1 — #39 + #40 closed) · PR C2 merged (#50, v1.7.2 — #47 + #49 closed; #49's replacement
convention was amended at implementation by owner decision: name:N locators — `ship:8`, `sdd:7.1.3` —
not spelled-out names, with the symbol ban in validate.py as filed). **Late addition #48** (filed
2026-07-11, an owner decision out of PR C's review pass) stays slotted below: drop legacy/backward-compat
support (backward compatibility is a non-goal — few installs, and removal simplifies the skills).

**This is the roadmap layer.** Each issue below is already spec-grade (acceptance criteria + file lists in the issue body). When an issue is picked up, write its per-issue implementation plan from that AC — this document decides only *order, grouping, risk posture, and guardrails*.

**Evidence base (all context-budget issues):** feature `0009-guardian-invite` — 231.8M cache-read tokens across 585 turns (~396K/turn) vs 2.13M of subagent work; every token entering the orchestrator's context is re-read ~75×.

---

## Decoder: internal numbering used inside the issue bodies

The `context-budget` issues cross-reference each other with the numbering of the original analysis doc, not GitHub numbers:

| In-body ref | GitHub issue |
|---|---|
| #1 | #36 trim tool output |
| #2 | #37 read discipline |
| #3 | #38 resolve routing once |
| #4 | #39 dev step 2 → reference |
| #5 | #40 dedup rules |
| #6 | #41 compress prose |
| "timing / per-turn-context probe (separate issue)" | #35 |

---

## #36 vs #42 — related, not duplicates (keep both open)

They belong to the same family — **runtime-generated transcript weight**, as opposed to the static loaded-file weight of #38–#41 — and cite the same evidence run. But they attack **different token streams** and have zero content overlap:

| | #36 trim tool output | #42 compress monologue |
|---|---|---|
| Token stream | **Input**: tool/command results (test logs, whole diffs, file reads) the orchestrator retains in its transcript | **Output**: reasoning the orchestrator + subagents *generate* each turn before their compact report |
| Fix | Redirect output to `.logs/`, read exit code + tail; diff by `--stat` + hunk | One compact-reasoning directive per charter/skill, with carve-outs for hard steps |
| Files | `workflow.md`, `build` §2, `ship` §1/§8, `wi-directory.md` — **no agent charters** | `agents/*` (all three charters) + `build`/`dev`/`rpa` skills |
| Compounding | Full ~75× (retained tool output is re-read every later turn) | Orchestrator-side: same ~75× (its own monologue stays in its transcript). Subagent-side: linear (transcripts are discarded) |
| Risk | Low — no rule content touched | Medium — over-compression can dull hard steps; needs a **real** run to validate |

They reinforce each other (less retained output → less to narrate about; less narration → less retained transcript) but neither subsumes the other. **Verdict: keep as two issues, land #36 early (Pile 1) and #42 late (careful-refactor), and never have both open against `build/SKILL.md` at the same time.** (#42's stated counterpart is actually #41 — loaded prose vs generated prose; #36 is the *third* leg: retained tool output.)

## The full taxonomy (why the order below)

1. **Measurement** — #35 (wall-clock timing). Everything else wants a before/after number.
2. **Run-state / transcript weight** (Pile 1, rule-free, safe): #36 tool output, #37 re-reads, #38 repeated reference consultation.
3. **Instruction weight** (Pile 2, moves/rewrites rule text, needs rules inventories): #39 relocate, #40 dedup, #41 compress.
4. **Generated weight** (careful, needs a real run): #42.
5. **Structure & support surface** (late additions, 2026-07-11): #47 reference layering and #49 citation
   convention are mechanical housekeeping; #48 legacy-support removal deletes rule text outright (a
   sanctioned behavior change, unlike the lossless piles) and belongs before #41 — delete before
   compressing, the same ordering logic as dedup-before-compress.

---

## Priority & risk order

| # | Issue | Effort | Risk | Saving | Why this slot |
|---|---|---|---|---|---|
| 1 | **#35** timing in `progress.md`/`tokens.md` | S–M | **Low** (additive; mirrors the token-ledger pattern) | n/a — observability | Named by #36 as the measurement prerequisite; every later PR's "before/after" claim uses it. Also a user-facing feature on its own. |
| 2 | **#36** trim tool output | S–M | **Low** | High | "Do first" per the issue: pure transcript hygiene amplified ~75×, zero rule content. |
| 3 | **#37** orchestrator read-discipline | M | **Low–Med** | High | Pairs with #36 — same `workflow.md` §Token budget anchor, same skills. Land in the same PR/stack slot. |
| 4 | **#38** resolve model routing once | S–M | **Low** | Medium | Independent quick win; closes Pile 1. |
| — | **Checkpoint A**: dry-run + `token_report.py` + #35 timing vs pre-sweep baseline | | | | |
| 5 | **#39** dev step 2 → `references/feature-folder-cases.md` | M | **Low–Med** (classifier must route; dry-run all six branches) | Med–High | Pile 2 opener; establishes the relocation template #41 reuses. |
| 6 | **#40** dedup restated rules | M–L | **Medium** (standalone-correctness of every touched file) | Medium + drift-risk removal | Dedup before compressing — otherwise #41 compresses text #40 would delete. |
| 7 | **#47** workflow.md → top-level `references/` + **#49** drop `§` citations | S | **Low** (mechanical; validate-guarded — #47 by the broken-ref check, #49 by a new `§` ban) | n/a — layering correctness + owner convention | One housekeeping PR right after C merges, before D branches: both rewrite the same citation strings, so doing them together touches each line once. |
| 8 | **#42** compact runtime reasoning | S–M | **Medium** (quality on hard steps; real-run gate) | Med–High (concentrated in build's task-runner waves + checker rounds) | After Pile 1 so the real-run baseline is clean; before #41 so its directive is in place when the big prose rewrite runs its rules inventory. |
| — | **Checkpoint B**: one **real** `/wi:dev` run, snapshot-diffed against a frozen pre-#42 baseline (same decisions/gates/reports, shorter reasoning) | | | | |
| 9 | **#48** drop legacy/backward-compat support | M | **Low by decree** (compat declared a non-goal; the issue's stays-list guards self-repair behaviors) | Medium instruction-token cut + simpler ledger gate | Deletes rule text outright, so it runs before #41's compression passes — no point compressing text #48 removes. Behavior change → **minor** bump. |
| 10 | **#41** compress skill prose | L | **Medium** (judgment-heavy; strongest guardrails) | Med–High, incremental | Last by its own design: benefits from #35 (measure per pass), #39 (template), #40 (dedup), #42 (directive already present), #48 (legacy text already gone). One file per PR. |
| ⏸ | #34 cross-vendor MoA proposers | — | — | — | **Postponed** (user decision, 2026-07-10). |

## PR / version grouping

Stacked PRs, squash-merge, rebase + retarget the next after each merge (same convention as #30→#31→#32). Bump `plugin.json` **and** the plugin entry in `.claude-plugin/marketplace.json` (line ~15; the top-level `0.1.0` is the marketplace schema, leave it). Minor bump when behavior/artifacts change, patch for pure relocation/compression:

| PR | Issues | Version | Notes |
|---|---|---|---|
| A | #35 | v1.6.0 | **Merged** (#44). Changes `tokens.md`/`progress.md` formats + ship gate → minor. |
| B | #36 + #37 + #38 | v1.7.0 | **Merged** (#45). Pile 1 as one PR (heavy file overlap between #36/#37 makes separate PRs conflict-prone); orchestrator behavior changes → minor. |
| C | #39 + #40 | v1.7.1 | **Merged** (#46). Lossless relocation/dedup, no behavior change → patch. Do #39's commits first, then #40's (dedup sweeps the post-#39 text). |
| C2 | #47 + #49 | v1.7.2 | **Merged** (#50). Housekeeping: pure file move + citation-convention sweep → patch; #49's convention amended at implementation to name:N locators (owner decision, PR #50). |
| D | #42 | v1.8.0 | Agent runtime behavior directive → minor. Merges only after Checkpoint B passes. |
| D2 | #48 | v1.9.0 | Removes supported behavior (legacy migrations, 4-column ledger acceptance, validate.py carve-out) → minor. After D, before E. |
| E1..En | #41 | v1.9.1+ | **One file per PR** (mandated by the issue): ship → research → build → dev, then rpa/scan. Each independently revertible, each with its rules inventory + loaded-token delta. |

## File-touch matrix — where to be careful

Touch counts across the sweep (✏ = modifies, ➕ = creates):

| File | #35 | #36 | #37 | #38 | #39 | #40 | #41 | #42 |
|---|---|---|---|---|---|---|---|---|
| `skills/build/SKILL.md` (104 ln) | ✏ | ✏ §2 | ✏ | ✏ | | ✏ | ✏ | ✏ |
| `skills/ship/SKILL.md` (379 ln) | ✏ §8 | ✏ §1/§8 | ✏ | ✏ | | ✏ | ✏ | |
| `skills/dev/SKILL.md` (128 ln) | | | ✏ | ✏ step 1 | ✏ step 2 | ✏ | ✏ | ✏ |
| `skills/research/SKILL.md` (170 ln) | ✏ | | ✏ | ✏ | | ✏ | ✏ | |
| `skills/rpa/SKILL.md` (184 ln) | | | | ✏ step 2 | (opt) | | later | ✏ |
| `skills/scan/SKILL.md` (284 ln) | | | ✏ lean-file warn | | | | later | |
| `…references/wi-directory.md` (213 ln) | ✏ templates | ✏ `.logs/` | ✏ | ✏ routing block | ✏ xref | ✏ canonical owner | | |
| `…references/workflow.md` (89 ln) | ✏ | ✏ house rule | ✏ budget→rule | | | ✏ canonical owner | | |
| `references/models.md` (195 ln) | | | | ✏ dispatch rule | | | | |
| `references/moa.md` (93 ln) | ✏ | | | | | | | |
| `agents/wi-task-runner.md` (124 ln) | | | | | | ✏ pointer | | ✏ directive |
| `agents/wi-researcher.md` (173 ln) | | | | | | | | ✏ directive |
| `agents/wi-code-checker.md` (163 ln) | | | | | | ✏ pointer | | ✏ directive |
| `skills/ship/scripts/_ledger.py` + `check_tokens.py` + `token_report.py` | ✏ | | | | | comments | | |
| `tests/test_tokens_guardrail.py` | ✏ | | | | | | | |
| `references/feature-folder-cases.md` | | | | | ➕ | | | |
| `docs/wi-design-notes/` | | | | | | | ➕ | |
| `…references/integrations.md` | | | | | | ✏ canonical owner | | |

**Hotspots:** `build/SKILL.md` (7 issues), `ship/SKILL.md`, `dev/SKILL.md`, `wi-directory.md` (6 each), `workflow.md` (4). This is why the stack is strictly serial — never two branches editing these concurrently.

**Late additions' touch surfaces (not folded into the matrix above — all three overlap the hotspots, so
the strictly-serial rule applies unchanged):**
- **#47** — `git mv` of `workflow.md` to `references/` + its literal-path citation sites (research, scan,
  models.md, rpa's build-uipath / build-maestro / uipath-bootstrap); validate.py's broken-ref check guards
  the sweep mechanically.
- **#49** — 143 `§` occurrences across 20 shipped files (heaviest: ship 22, rpa 15, brainstorm-protocol
  15, sdd-template 13; incl. `wi-code-checker.md` ×5 — symbol swap only) + validate.py (retarget the
  `§13` dead-anchor lint, add the `§` ban).
- **#48** — dev, `references/feature-folder-cases.md`, models.md, rpa (+ ingest / build-uipath), build
  (+ worktrees-and-subagents), ship, wi-directory, `scripts/validate.py` 7c, `_ledger.py` /
  `check_tokens.py` + `tests/test_tokens_guardrail.py`.

**Agent charters (`agents/*.md`) are the most sensitive surface** — they steer autonomous subagents with no human in the loop. Only two issues may touch them, both with minimal additive diffs:
- #40: replace duplicated rule restatements with one-line pointers — nothing else.
- #42: add one compact-reasoning directive + the hard-step carve-outs — nothing else.
- **Never** alter report caps (~15/~20 lines), output markers, verification-gate contracts, or tool lists in either.

## Guardrails (apply per PR)

1. **Rules inventory** before/after for every PR that moves or rewords rule text (#39, #40, #41; #42's carve-out list counts). Attach it to the PR body. Test: *does each touched file still make correct decisions if loaded alone?*
2. `python scripts/validate.py` + `pytest tests/` green; after bulk markdown edits, check file **tails** (this repo has shipped mid-sentence truncated files before; `validate.py` only guards trailing-newline + fence balance).
3. Dry-run per pile; **Checkpoint B for #42 must be a real run**, not a dry-run — a simulation shares the model's blind spots.
4. New files only in already-whitelisted dirs (`references/`, `docs/` — both in `.gitignore`'s `!` list). Any *new top-level* path needs a `!/path` line or it silently vanishes.
5. `.wi/features/<slug>/.logs/` (#36) is a *user-project* path: it goes in wi-directory's strays/prune list + the scan-generated gitignore guidance, not this repo's `.gitignore`.
6. #38's resolved-routing block caches what `.wi/models.md` says — defaults stay opus/sonnet; never bake a session's top-tier model into the resolution. Settle the staleness rule at pick-up (recommend: re-resolve when the block is absent **or** `.wi/models.md` changed since it was written).
7. #35 backward compatibility: `check_tokens.py`'s new duration gate must accept legacy `tokens.md` (no Duration column) and `unavailable` values — old features' dossiers must not start failing ship. **Superseded by #48** (2026-07-11, owner decision): once #48 lands, the gate accepts only the current Duration-column format and this guardrail retires with the other compat shims.
8. No AI attribution in commits/PRs (repo convention).
9. **#48's stays-list is binding** (it's in the issue): "legacy" describing the *user's* code or target
   systems stays (wi-researcher, research step 1b, rpa connectors); self-repair behaviors keep their
   mechanism and lose only the pre-1.x framing (absent routing block → re-resolve; missing `Flow:` →
   `dev`; build-uipath's out-of-order-resume dossier check); graceful `unavailable` on bad stamps stays.
   The dev classifier (five classes) and `feature-folder-cases.md` (four sections) change together.
10. **#49 in agent charters:** symbol swaps only — never alter report caps, output markers,
    verification-gate contracts, or tool lists (the standing charter rule).

## Per-issue pick-up notes

- **#35** — adopt the issue's five design recommendations as written: headline total = **autonomous wall-clock** via sum-of-phase-spans (drops gate waits + resume idle for free), ISO-8601 with offset, OS clock only, `unavailable` over estimates, gate labels "gate 1 → gate 2" / "gate 2 → end". `_ledger.py` stays the single owner of the `tokens.md` byte format.
- **#36/#37** — both hang off `workflow.md` §Token budget (line ~80): #36 adds the redirect-and-tail house rule, #37 promotes the existing "hold at most…" sentence to a hard rule each phase skill cites. Write them as one coherent section edit.
- **#38** — dry-run must show *identical* model assignments for the same `.wi/models.md` before/after.
- **#39** — the six branches move **verbatim**; dry-run each branch type (new / resume / in-flight overlap / done-collision / roadmap-row / legacy). Audit `rpa` step 2 for the same factoring in the same PR only if it's mechanical; otherwise defer to #41's rpa pass.
- **#40** — first commit is the rules-map (rule → canonical owner → pointer sites); owners per the issue: token honesty → `wi-directory.md`/`_ledger.py`, gates → `workflow.md`, frontend delegation → `integrations.md`, `.wi/` commit rule → `wi-directory.md`. Note the frontend-delegation duplication evidence also names `skills/scan/references/plugin-bootstrap.md` and the `.wi/`-commit evidence names `skills/rpa/references/build-uipath.md`, though neither appears in the issue's files-to-touch list — include both in the rules-map. Then sweep grep for further duplicates beyond the four confirmed ones.
- **#42** — carve-outs that keep full reasoning: researcher tech-choice, checker adversarial passes, plan decomposition, design gates. Prefer the shared-reference form (one `references/` note cited by all six touch points) over repeating the directive 6× — that's #40's lesson applied proactively.
- **#41** — order by loaded-per-run impact: ship (379) → research (170) → build (104) → dev (128), then rpa/scan. Rationale moves to `docs/wi-design-notes/<skill>.md`, linked by one line. Report the loaded-token delta per pass using #35's infra + `token_report.py`.
- **#47** — pure `git mv` + literal-path sweep; validate.py's broken-ref check is the mechanical proof.
  Scoped to workflow.md only; `wi-directory.md` / `integrations.md` share the pattern but get a sibling
  issue later if wanted, not a scope grow here.
- **#48** — inventory first (grep `legacy` / `pre-1.` / `pre-rename` / `old-named` / `old filename`),
  classify every hit against the issue's stays-list, then delete the seven compat families; PR body
  carries the deleted-vs-relabeled disposition table. Dry-runs: old-named models config → fresh setup;
  a pre-rename work-unit folder → treated as unscanned; current-format flows unchanged.
- **#49** — one rule per citation shape: named sections → quoted headings (`workflow.md "Script
  invocation"`), skill steps → `ship step 8`, SDD ToC → `section 10`. Land in the same PR as #47 so the
  moved file's citations are written once, already in the new convention.

## Out of scope

- #34 cross-vendor MoA proposers — postponed; revisit after the sweep (it touches `cross_review.py` + `moa.md`, which #35 also edits — landing #35 first actually simplifies #34 later).
- Promoting `wi-directory.md` / `integrations.md` to top-level `references/` — #47's sibling candidates;
  decide after #47 lands, as their own issue (they are cited far more widely, including from agent charters).
- Recovering timing for past runs; per-tool-call metrics (#35's own exclusions).
- The "fresh orchestrator context per phase, rehydrated from progress.md" structural idea — #37 names it as a possible follow-up issue, not part of this sweep.
